/**
 * HTTP Extraction Module
 * 
 * Provides HTTP-based product extraction capabilities for sites that
 * serve JSON-LD structured data in their initial HTML response.
 */

const HTTPJsonLdExtractor = require('./HTTPJsonLdExtractor');

module.exports = {
  HTTPJsonLdExtractor
};